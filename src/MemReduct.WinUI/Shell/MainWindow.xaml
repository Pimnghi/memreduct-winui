<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="MemReduct.WinUI.Shell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Title="Mem Reduct WinUI"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="TitleBarRow" Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" VerticalAlignment="Stretch" Background="Transparent">
            <Image x:Name="TitleBarIcon" Source="ms-appx:///Assets/AppIcon.ico" Width="24" Height="24" Margin="12,0,0,0"
                   HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBlock x:Name="TitleLabel" Text="Mem Reduct WinUI" VerticalAlignment="Center"
                       Margin="48,0,0,0" HorizontalAlignment="Left"
                       FontSize="12" 
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
        </Grid>

        <controls:NavigationView x:Name="NavView" Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            IsPaneToggleButtonVisible="True"
            PaneDisplayMode="Left"
            IsPaneOpen="False"
            OpenPaneLength="240">

            <controls:NavigationView.MenuItems/>

            <controls:NavigationView.FooterMenuItems>
                <controls:NavigationViewItem x:Name="NavMemory" Tag="main" Content="Memory">
                    <controls:NavigationViewItem.Icon>
                        <SymbolIcon Symbol="ViewAll"/>
                    </controls:NavigationViewItem.Icon>
                </controls:NavigationViewItem>
                <controls:NavigationViewItem x:Name="NavSettings" Icon="Setting" Content="Settings" Tag="settings"/>
                <controls:NavigationViewItem x:Name="NavAbout" Content="About" Tag="about">
                    <controls:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" FontSize="16"/>
                    </controls:NavigationViewItem.Icon>
                </controls:NavigationViewItem>
            </controls:NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </controls:NavigationView>
    </Grid>
</Window>
